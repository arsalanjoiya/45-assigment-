"use strict";
let transportation = ['civic', 'fourtuner', 'bmw', 'mark x', 'land cruser'];
for (let i = 0; i < transportation.length; i++) {
    console.log('I would like to own a ' + transportation[i]);
}
